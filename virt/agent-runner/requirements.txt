# Base deps for the guest computer-use runner. The vendored/forked Agent S
# (gui_agents) and its own requirements are installed separately into the venv.
pyautogui>=0.9.54
pillow>=10.0
mss>=9.0
pynput>=1.7
openai>=1.0        # OpenRouter planner client (OpenAI-API compatible)
requests>=2.31
httpx>=0.27
pyyaml>=6.0
